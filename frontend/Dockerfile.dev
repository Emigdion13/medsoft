FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

# Copy .env.docker to .env for Vite to load
RUN if [ -f .env.docker ]; then cp .env.docker .env; fi

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
